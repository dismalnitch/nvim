return {
  "github/copilot.vim",
  event = "InsertEnter", -- loads on first insert, change as needed
  config = function()
    -- No config needed for basic use.
    -- For advanced setup, see plugin docs.
  end,
}
